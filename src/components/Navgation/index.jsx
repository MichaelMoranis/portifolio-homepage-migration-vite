import ThemeToggle from "../Button";
import MenuNavigation from "../Menu";

function Navigation() {
  return (
      <div>
        <MenuNavigation />
        <ThemeToggle />
      </div>
  );
}

export default Navigation;
